你是资深前端工程师，在现有 Next.js 14 + React 18 + TypeScript + Ant Design + Tailwind + TanStack Query 项目中实现“资讯分析”模块。请直接修改代码，不要只输出建议。

项目根目录：/Users/lushuailei/PycharmProjects/ai
前端目录：/Users/lushuailei/PycharmProjects/ai/web

目标：新增独立页面 `/workstation/news-analysis`，并对接后端接口 `/api/v1/news-analysis/*`，保持可扩展、可复用。

必须遵循：
1) 左侧资讯列表 + 右侧 AI 智能分析双栏布局。
2) 分类筛选：全部、快讯、公告、研报。
3) 只看重要开关。
4) 热门股票标签，点击后过滤资讯。
5) 每条资讯支持“AI解读”入口（按钮即可，先做交互占位）。
6) 右侧卡片展示：24小时热讯解读、热点追踪、宏观影响、个股解读。
7) 热门资讯榜单展示。
8) 完整加载态/空态/错误态。
9) 代码风格与现有 `web/features/preopen` 保持一致。

接口约定（统一包 success/data）：
- GET /news-analysis/feed?category=all|flash|announcement|report&important_only=bool&stock_code=string
- GET /news-analysis/ai-panels
- GET /news-analysis/hot-stocks
- GET /news-analysis/hot-news
- GET /news-analysis/by-stock/{stockCode}/summary

实现范围建议：
- 新建 `web/types/news-analysis.ts`
- 新建 `web/features/news-analysis/api/index.ts`
- 新建 `web/features/news-analysis/hooks/index.ts`
- 新建 `web/features/news-analysis/components/*`
- 新建页面 `web/app/(workstation)/workstation/news-analysis/page.tsx`
- 更新路由常量与左侧导航（将资讯分析指向该真实路径）

技术要求：
- 使用 React Query 查询。
- API 层统一通过 `http` 方法。
- 类型尽量完整，不使用 any。
- 组件有适当注释（仅复杂逻辑处）。
- 保持当前项目可通过 typecheck/lint/build。

完成后请给出：
- 修改文件列表
- 注意事项（若与后端契约存在差异要标出）
