"""API v1 package.

The router is assembled by ``router_factory.create_api_v1_router()`` and
included by ``app_factory.create_app()``.  Import ``api_router`` from
``app.api.v1.router`` only if you need the backward-compatible singleton.
"""
