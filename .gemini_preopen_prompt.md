你是资深前端工程师。请基于 Next.js 14 + React18 + TypeScript + Antd + Tailwind + echarts-for-react，完成“盘前速览”页面落地。

目标：
- 主页面：web/app/(workstation)/workstation/page.tsx
- 如有需要可新增文件到：
  - web/features/preopen/api/*
  - web/features/preopen/hooks/*
  - web/features/preopen/components/*
  - web/types/*
- 不要改后端文件。

必须对接的后端接口（真实调用，不能写死数据）：
- GET /preopen/hot-news
- GET /preopen/ai-digest
- GET /preopen/market-indicators
- GET /preopen/anomalies
- GET /preopen/trends
- GET /preopen/limit-up-ladder

实现要求（严格）：
1) 页面需包含：
- A股热讯榜
- AI 热讯解读卡
- 市场指标卡（至少6项）
- 异常名单（尾盘异动/严重异常）
- 15日趋势图（可切换至少2个维度）
- 涨停天梯区
2) 状态完整：加载态、空态、错误态、非交易日提示态。
3) 代码可维护：
- API 层与页面分离
- hooks 管理请求
- 组件拆分，避免 page.tsx 过长
4) UI 高信息密度，桌面优先，同时移动端不崩。
5) 严禁新增第三方依赖。
6) 保持与现有代码风格一致。

额外要求：
- 使用现有 http 工具（web/lib/request/http-client.ts）。
- 使用已有 QueryProvider（可通过 useQuery 实现）。
- 如果后端字段缺失，做 defensive render，不要让页面崩溃。

完成后请直接修改代码，不要只给方案。
