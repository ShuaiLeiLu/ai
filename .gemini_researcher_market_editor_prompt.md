你是资深前端工程师，请在当前 Next.js 14 + React 18 + TypeScript + Ant Design + Tailwind + TanStack Query 项目中实现「人才市场 + 研究员编辑器」。请直接改代码。

项目：/Users/lushuailei/PycharmProjects/ai
前端目录：/Users/lushuailei/PycharmProjects/ai/web

目标页面：
1) /workstation/researcher-market
2) /workstation/researcher-editor

后端接口（/api/v1 前缀已由 http 注入）：
- GET /researchers/market?q=&page=&page_size=
- GET /researchers/market/{researcher_id}
- GET /researchers/mine
- POST /researchers/{researcher_id}/hire
- POST /researchers/{researcher_id}/dismiss
- POST /researchers/{researcher_id}/duplicate
- POST /researchers/{researcher_id}/publish
- POST /researchers/{researcher_id}/unpublish
- GET /researchers/options/skills
- GET /researchers/options/knowledge-bases
- GET /researchers/options/mcp-servers
- GET /researchers/{researcher_id}
- PATCH /researchers/{researcher_id}
- POST /researchers
- POST /researchers/{researcher_id}/test-chat

实现要求：
- 人才市场页：搜索栏、研究员卡片、雇佣/解雇、查看详情、分页。
- 研究员编辑器页：
  - 基本信息（名称/头像/介绍）
  - 提示词编辑
  - 技能/知识库/MCP 选择（多选）
  - 自驱任务（可先做简化列表+添加）
  - 测试对话区（调用 test-chat 接口）
  - 保存与发布区分按钮
- 完整加载/空态/错误态。
- 组件化拆分，新增 types/api/hooks/components。
- 不引入新依赖，不使用不存在组件。
- typedRoutes 兼容。

建议文件：
- web/types/researcher-market.ts
- web/features/researcher-market/*
- web/features/researcher-editor/*
- web/app/(workstation)/workstation/researcher-market/page.tsx
- web/app/(workstation)/workstation/researcher-editor/page.tsx
- 更新 routes 与快捷入口跳转（让“招募研究员/创建研究员”可进入对应页面）

完成后输出：
- 修改文件列表
- 假设字段（如与后端不一致请说明）
