你是资深前端工程师，请在当前 Next.js 14 + React 18 + TypeScript + Ant Design + Tailwind + TanStack Query 项目里实现“任务编排与自驱任务”模块。请直接改代码。

项目：/Users/lushuailei/PycharmProjects/ai
前端：/Users/lushuailei/PycharmProjects/ai/web

目标页面：`/workstation/tasks`（新增页面）

后端接口（/api/v1 前缀已在 http client 注入）：
- GET /tasks
- POST /tasks
- PATCH /tasks/{task_id}
- DELETE /tasks/{task_id}
- POST /tasks/{task_id}/activate
- POST /tasks/{task_id}/pause
- POST /tasks/{task_id}/run
- GET /tasks/runs?task_id=
- GET /tasks/{task_id}/runs
- GET /tasks/runs/{run_id}/logs

页面目标：
1) 任务列表区：展示任务名称、调度类型、创建时间、状态、最近执行状态。
2) 创建/编辑任务弹窗：
- schedule_type 支持 one_time/interval/cron
- 交易日开关、强制输出文档开关
- description、prompt_template
- 显示动态变量提示（{{date}}/{{lastDate}}/{{nextDate}}/{{lastTradeDate}}/{{nextTradeDate}}）
3) 执行历史区：展示 runs 列表，可查看某次 run 的 logs。
4) 操作：启用、暂停、执行一次、编辑、删除。
5) 状态：加载态、空态、错误态、运行中态。

建议文件：
- web/types/tasks.ts
- web/features/tasks/api/index.ts
- web/features/tasks/hooks/index.ts
- web/features/tasks/components/*
- web/app/(workstation)/workstation/tasks/page.tsx

硬性要求：
- 不引入新依赖。
- 保持 typedRoutes 兼容，不写不存在路径跳转。
- 接口调用统一 `http`。
- 类型完整，避免 any。
- 与现有代码风格一致。

完成后输出：
- 修改文件列表
- 若接口字段有假设请列出
